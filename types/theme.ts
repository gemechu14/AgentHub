export interface EmbedTheme {
  primary: string;
  accent: string;
  background: string;
  surface: string;
  textPrimary: string;
  border: string;
  success: string;
}

export const DEFAULT_THEME: EmbedTheme = {
  primary: "#0F172A",
  accent: "#3B82F6",
  background: "#F8FAFC",
  surface: "#FFFFFF",
  textPrimary: "#0F172A",
  border: "#E2E8F0",
  success: "#22C55E",
};

